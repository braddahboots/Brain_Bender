'strict mode';

  // Creat a function
 function circString( source, find ) {
  var sourceWord = str.concat(source + source);
  if(sourceWord.indexOf(find)) {
      return true;
  }
}

console.log(circString(Hotdog, dog);


  // Invokes an algorithm
  // Algorithm takes 2 string ( source, find )
  // Function should return "true" if the string being passed through ( find )...
  // is found in the ( source )
  // Only if ( source ) is circular in nature = there is no end to the ( source ) string
  // A match (between ( find ) and ( source ) is true if the word to find is...
  // partially (look up) at the end of the word and at the beginning in sequence.

  // Ex ( souce = Hotdog, find = dog --- true ) ( source = batman, find = bat --- true)

